namespace SliceOfPie_Model {

    /// <summary>
    /// Synchronization enum types. Used to determine action for each filelistentry.
    /// </summary>
  public enum FileListType {
    Conflict,
    Push,
    Pull
  };
}
